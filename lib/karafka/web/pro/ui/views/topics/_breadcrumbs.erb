<li>
  <a href="<%= root_path('topics') %>">
    Topics
  </a>
</li>

<% if @topic %>
  <li>
    <a href="<%= root_path('topics', @topic.topic_name, 'config') %>">
      <%= @topic.topic_name %>
    </a>
  </li>
<% end %>

<% if @configs %>
  <li>
    <a href="<%= root_path('topics', @topic.topic_name, 'config') %>">
      Configuration
    </a>
  </li>
<% end %>

<% if @partitions %>
  <li>
    <a href="<%= root_path('topics', @topic.topic_name, 'replication') %>">
      Replication
    </a>
  </li>
<% end %>

<% if @distribution %>
  <li>
    <a href="<%= root_path('topics', @topic.topic_name, 'distribution') %>">
      Distribution
    </a>
  </li>
<% end %>

<% if @offsets %>
  <li>
    <a href="<%= root_path('topics', @topic.topic_name, 'offsets') %>">
      Offsets
    </a>
  </li>
<% end %>
