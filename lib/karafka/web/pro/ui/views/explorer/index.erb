<% view_title 'Explorer' %>

<% if @topics.empty? %>
  <%== partial 'explorer/no_topics' %>
<% else %>
  <div class="topic-tiles">
    <%==
      each_partial(
        @topics,
        'explorer/topic'
      )
    %>
  </div>
<% end %>
