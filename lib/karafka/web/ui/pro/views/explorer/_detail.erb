<% if v.is_a?(Hash) && !v.empty? %>
  <% v.each do |k2,v2| %>
   <tr class="align-middle">
      <td>
         <%= "#{k}.#{k2}" %>
      </td>
      <td>
        <%= v2 %>
      </td>
   </tr>
  <% end %>
<% else %>
  <tr>
    <td>
      <%= k %>
    </td>
    <td>
      <%= object_value_to_s(v) %>
    </td>
  </tr>
<% end %>
