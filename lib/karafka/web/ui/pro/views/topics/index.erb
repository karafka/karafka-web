<%== view_title('Topics', hr: true) %>

<div class="container">
  <% if @topics.empty? %>
    <%== partial 'explorer/no_topics' %>
  <% else %>
    <div class="row mb-5 row-cols-1 row-cols-md-4 g-4">
      <%==
        each_partial(
          @topics,
          'topics/topic'
        )
      %>
    </div>
  <% end %>
</div>
