<% view_title 'Cluster' %>

<%== partial 'cluster/tabs' %>

<% if Karafka.pro? && (filtering? || !@brokers.empty?) %>
  <div class="col-span-12 mb-3">
    <%== filter_box %>
  </div>
<% end %>

<% if @brokers.empty? && filtering? %>
  <div class="col-span-12">
    <%== partial 'shared/no_filter_results' %>
  </div>
<% else %>
  <%== partial 'cluster/brokers' %>
<% end %>
