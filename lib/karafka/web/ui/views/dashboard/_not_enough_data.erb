<% alert_box_info('There Needs to Be More Data to Draw Meaningful Graphs') do %>
  If you are sure you are running at least one <code>karafka server</code> instance and you do not see charts in a few minutes, please ensure that it can report to the <code><%= Karafka::Web.config.topics.consumers.reports.name %></code> topic and that Web-UI is correctly installed.
<% end %>
